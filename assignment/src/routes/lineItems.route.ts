import { Routes } from '@angular/router';
import { LineItemComponent } from '../lineItems/lineItem.component';

export const lineItemsRoutes: Routes = [
    {
        path:'',
        component:LineItemComponent
    }
]